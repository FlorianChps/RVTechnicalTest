<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/white">

    <TextView
        android:id="@+id/station_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/margin_large"
        android:layout_marginTop="@dimen/margin_xx_large"
        android:layout_marginBottom="@dimen/margin_xx_large"
        android:fontFamily="@font/poppins_regular"
        android:includeFontPadding="false"
        android:textColor="@color/green_200"
        app:layout_constraintBottom_toBottomOf="@+id/station_favorite"
        app:layout_constraintStart_toEndOf="@+id/station_favorite"
        app:layout_constraintTop_toTopOf="@+id/station_favorite"
        android:textSize="16sp"
        tools:text="Gare de l'Est" />

    <ImageView
        android:id="@+id/station_favorite"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/margin_large"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_star_empty" />
</androidx.constraintlayout.widget.ConstraintLayout>